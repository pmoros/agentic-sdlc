# Scripts

Reusable scripts produced during this session — anything worth running more than once or handing to someone else (data migration, log-pull, deploy helper, verification check).

Keep these runnable as-is: no hardcoded secrets (reference env vars / secret manager paths), and note required env vars at the top of the script or in a comment block.

One-off throwaway commands belong in `../tmp/`, not here.
